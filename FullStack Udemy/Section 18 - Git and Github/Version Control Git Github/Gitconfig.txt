#Hosting
-git config

Open Terminal:
    -git config --global user.name "Pradeep kumar Sharma"
    -git config --global user.email "pradeep@gmail.com"
    -git config --global user.name ====> give you username it okk Status 200
    -git config --global core.editor "code --wait"


.gitignore => A file which define all other files to not tracked by git one of the important file in development, many files like .env which we don't have to make public due to security regions, Many .gitignore generator are there on internet


Working of Tracking system or Version control System
-HEAD --> Master/Main  which mean your changes are tracked by Master or Main brach
-Every commit dependent on previous commit except first one

#Explore .git when initialization happens in repo

#checking previous commits
-

#Branches of git
-git init
-git branch, all branch lists
-git branch branch-name-bug112-fixing, create new branch with branch name
-git checkout branch-name-bug112-fixing, to change branch Head pointing to specified branch name
-git switch master, same to change branch
WRITE+STAGE(ADD)+COMMIT(CHECKPOINT)

#Marge Branches
example; you have two branch one master and other is bug112fixing and have to marge into main or master
-First come to Master
-git switch master
-git marge bug112fixing
HEAD --> master 

#TWO TYPES OF MARGE 
    -fast forward marge parent(master) has no changes and commits
    -not fast forward marge parent(master) is also changes which more then 1 commits


#git Tries to resolve conflicts